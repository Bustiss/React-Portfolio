import { Navigation } from './Navigation';

export const Header = () => {

    return (
        <div className="header">
        <h1 className="logo-font">LOGO</h1>
        <Navigation />
        </div>
    )
}